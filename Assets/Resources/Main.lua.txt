--用到时require
setmetatable(_G, {
    __index = function(t,i)
        local ret = require(i)
        t[i] = ret
        return ret
    end
})
function main( )
    --UIManager:Open("UIMain")
    UIManager:Init()
    UIManager:Open(UIMain)
    UIManager:Open('UIRight',"UIMain")
end

main()