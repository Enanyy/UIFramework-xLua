--用到时require
setmetatable(_G, {
    __index = function(t,i)
        local ret = require(i)
        t[i] = ret
        return ret
    end
})

function main( )
    --UIManager:Open("UIMain")

    local data = DTHero.Get(1)
    print(data.id)
    print(data.name)
    print(data.height)
    print(data.list)

    UIManager:Init()
    UIManager:Open(UIMain)
    UIManager:Open('UIFixed',"UIMain")
end

main()